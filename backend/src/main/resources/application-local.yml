# 本地开发 profile（gitignored —— 含真实凭据，严禁提交）
# 由 application.yml 的 spring.profiles.default=local 自动激活。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/star_rain_notes?useUnicode=true&characterEncoding=utf8&connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true&sslMode=DISABLED&allowPublicKeyRetrieval=true
    username: root
    password: "1234"
    driver-class-name: com.mysql.cj.jdbc.Driver
app:
  setup-token: dev-setup-token-123

<script setup lang="ts">import { computed } from 'vue'; const props=defineProps<{status:string}>(); const label=computed(()=>({PUBLISHED:'已发布',WITHDRAWN:'已撤回',DRAFT:'草稿'}[props.status]??props.status))</script>
<template><span class="publish-status" :class="`is-${status.toLowerCase()}`">{{ label }}</span></template>
<style scoped>.publish-status{display:inline-flex;align-items:center;min-height:25px;padding:3px 9px;border:1px solid var(--border);border-radius:999px;background:var(--bg-subtle);color:var(--text-muted);font-size:11px;white-space:nowrap}.publish-status.is-published{border-color:color-mix(in srgb,var(--primary) 30%,var(--border));color:var(--primary)}.publish-status.is-withdrawn{border-color:color-mix(in srgb,var(--accent) 35%,var(--border));color:var(--accent)}</style>
